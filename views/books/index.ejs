<!DOCTYPE html>
<html lang="en">
    <%- include('../partials/navbar') %> <%- include('../partials/head', {title: 'Books'})%> <% console.log(books) %> <% books.forEach(book => { %>
    <img src="<%= book.image %>" alt="" />
    <h4><%= book.title %></h4>
    <% }) %>
    <body></body>
</html>
